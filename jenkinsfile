@Library('singuvenkatesh/jenkins_shared') _

pipeline {
  agent any
    
  stages {
        
    stage('shared repository') {
      steps {
	  
    echo 'Hello world'
    sayHello 'singu'
    
         }

      }

            }

     }
